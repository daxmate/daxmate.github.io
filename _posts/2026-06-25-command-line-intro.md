---
layout: single
title:  "从零开始学命令行"
date:   2026-06-25
categories: "shell"
---

从我开始用 macOS 起，终端就是经常要用到的工具。但很长一段时间里，我对它的了解就仅限于"在这敲命令"——打开后就是一个黑框加一个闪烁的光标，更多能干什么，完全没概念。

命令行对大部分人来说都是望而生畏的：打开一个终端界面，除了简单的提示符以外什么都没有，你根本不知道它能做什么。但它确实是一个功能无比强大的工具，而且里面很多非常非常好用的工具都是免费的。

## 命令行是什么

把命令行拆开来看其实不复杂：终端（Terminal）是一个窗口程序，它在里面跑一个叫 Shell 的东西（最常见的是 Bash 或 Zsh），Shell 负责接收你敲的文字指令，交给操作系统去执行。

一开始会很不习惯 — 没有按钮点，没有菜单看，一个命令敲错了就是一行报错。但它的思路其实很简单：你说一句，它做一句。

## 命令行 vs 图形界面

同一个功能的工具，命令行套个图形界面壳就开始收费了，这样的软件有很多。所以最好先搞清楚它们分别擅长什么：

- **命令行**擅长那些有**明确规则**的操作 — 批量重命名、转换格式、搜索替换、定时任务，规矩定好了就交给它自动跑
- **图形界面**擅长那些必须靠**眼睛做出判断**的工作 — 比如用 Photoshop 修图，每一笔都需要你看效果来调整

命令行工具还有一个好处：**轻量、速度快**。没有花哨的动画，不吃内存，一个命令下去结果秒出。

## 可以拿来做什么

我自己也在摸索阶段，遇到过的感受最深的是这几类：

- **批量操作文件** — 一次性重命名几百个文件、把某种格式的文件全部移到另一个目录，一行命令秒完成。用鼠标一个个点能点到崩溃
- **自动化重复工作** — 每天备份、定时清理、自动拉取数据，脚本写好了就不用管了
- **连上远程服务器** — 服务器只有命令行没有界面，SSH 过去查日志、看进程、改配置，跟在本地一样
- **把小工具串起来用** — 一个工具的输出直接丢给另一个工具，组合出图形界面很难做到的定制化效果

## 我会接着写什么

接下来的几篇文章都是我边学边记的笔记，计划是这样的：

- [终端与 Shell 基础](/)
- [文件操作入门](/)
- [管道和重定向](/)
- [第一个 Shell 脚本](/)

命令行最大的门槛不是难，是刚开始不习惯。给自己一两天试试，说不定就回不去了。
