
---
title: "命令行系列 #1：为什么你该学学命令行"
date: 2025-01-01
categories:
  - Blog
tags:
  - 命令行
  - bash
---

我是用电脑的，Windows、macOS、Linux 都用。写了几年代码之后，回头想想，有一样东西对我工作效率的提升是最大的——不是某个编辑器，不是某个框架，而是**命令行**。

你可能觉得命令行是程序员才用的东西。黑乎乎的窗口，一堆看不懂的字母，看起来就吓人。

我以前也这么想。后来发现，命令行其实是**给电脑下指令最直接的方式**。鼠标点来点去，是你在模仿电脑的思维方式。敲命令，是电脑在听你的。

这个系列就是想把这些东西讲清楚。不需要你是程序员，只要你平时用电脑、想用得更好，就够了。

---

## 为什么要学命令行？

### 1. 快

重复的事情，点鼠标要 10 秒，命令行 1 秒。

比如你有一个文件夹，里面有好几百个文件，你想把所有文件名里的 `2020` 改成 `2024`。用鼠标一个个改？累死。用命令行？一句话的事。

### 2. 批量

一个命令可以对几百个文件同时操作。复制、改名、搜索、统计……手指一敲就完事。

### 3. 可重复

你今天做了一件事，明天还要做一遍。鼠标操作你得记住每一步点在哪儿。命令行？把昨天那条命令再跑一遍就行。

还可以写成脚本，定时自动执行。

### 4. 控制力

图形界面给你什么按钮你就用什么。命令行里，你能做到 GUI 根本做不到的事。

---

## 你需要准备什么

如果你是 **Mac 用户**——恭喜，自带的「终端」(Terminal) 就能用，而且默认是 `zsh`，非常强大。

如果你是 **Windows 用户**——建议装个 [Git Bash](https://git-scm.com/) 或者直接用 WSL（Windows Subsystem for Linux）。这个系列后面的例子主要基于 `bash`，在 `zsh` 下也基本通用。

打开你的终端，看到那个闪烁的光标了吗？那就是电脑在等你下命令。

先试一行最简单的：

```bash
echo "Hello, world!"
```

按回车，看到什么了？电脑回了一句 Hello, world! 给你。

这就是命令行——你说一句，它回一句。

---

## 这个系列要讲什么

- **第一篇（本篇）**：为什么命令行值得学 + 基本概念
- **第二篇**：文件操作——移动、复制、删除、搜索
- **第三篇**：文本处理——grep、sed、awk，让数据乖乖听话
- **第四篇**：管道和重定向——把命令连起来用
- **第五篇**：写你的第一个脚本——把重复的工作自动化
- ...

每一篇都短一点，读得完、用得上。这样才有动力继续往下看。

---

## 一个小实验

你现在打开终端，输入：

```bash
ls -la
```

回车。看到了什么？一堆文件信息对吧？`ls` 是 list 的缩写，`-la` 是选项，意思是「列出全部详细信息」。

你刚才办到了鼠标点击好几下才能看到的事。

这就是开始。

---

*下一篇：文件操作——mv、cp、rm 怎么用不翻车*
